public class time_complexicity_shortcut {
    public static void main(String[] args) {
        /*
         * the common patterns..
         * iterating over an array or list---- O(n);
         * nested loops --- O(n) 
         * if two nested loop --- O(n^2);
         * if three nested loop --- O(n^3);.....etc....
         * 
         * binary Search --- O(log n) ---especially when the data is sorted....
         * 
         * 
         * binary search--- a loop thats iterative like reduce it to half after each itetations like binary search usually has O(log n);
         * 
         * 
         * 
         * 
         */
    }
}
