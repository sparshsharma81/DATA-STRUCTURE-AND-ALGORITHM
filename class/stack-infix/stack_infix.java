public class stack_infix{
    
    public static void main(String args[]){
        //now we are doing the infix postfix in 
        /*
         while pushing the operator into stack, if current operator has less or equal precedence then peek(),
         then pop() the stack and append it to the postfix string, repeat this until the current operator's precedence is greater than the peek() or
         stack is empty

         */
    }
}