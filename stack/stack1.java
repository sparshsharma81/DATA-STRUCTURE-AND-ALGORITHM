import java.util.Stack;

public class stack1{
    public static void main(String args[]){
        /*
         stack is a linear data structure which follow lifo format
         LAST IN FIRST OUT
         the buildin class in java is java.util.Stack;
         push(x) --- this is used to push the element at the top
         pop --- used to remove the top element
         peek --- this will display the top element without removing it
         isEmpty() --- returns true or false if stack is empty or not
         search(x) ---returns the positon of x from top (1-based indexing)
         */
        Stack<Integer>s1 = new Stack<>();
        s1.push(10);
        s1.push(20);
        st.push(30);
        st.push(40);
        System.out.print(s1); //----[10, 20]
        
    }

}